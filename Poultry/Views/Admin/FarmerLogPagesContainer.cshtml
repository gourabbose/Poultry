@model IEnumerable<Poultry.Models.FarmerLog>
@if (Model.Count() > 0)
{
    foreach (var v in Model)
    {
        Html.RenderPartial("FarmerLogPages", v);
    }
}
else
{
    <strong>No Data Found.</strong>   
}
