@model IEnumerable<Poultry.Models.VendorLog>
@if (Model.Count() > 0)
{
    foreach (var v in Model)
    {
        Html.RenderPartial("VendorLogPages", v);
    }
}
else
{
    <strong>No Data Found.</strong>   
}
